<aura:component >
    <aura:attribute name="name" type="list"/>
    
    <lightning:button label="Create Map from List" onclick="{!c.convert}"/>
    <br/>
    <aura:iteration items="{!v.name}" var="item">
        key : {!item.key} value: {!item.value} <br/>
    </aura:iteration>
</aura:component>